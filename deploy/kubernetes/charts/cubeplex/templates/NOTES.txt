cubeplex has been deployed.

  Namespace: {{ .Release.Namespace }}
  Release:   {{ .Release.Name }}

{{- if .Values.ingress.enabled }}
Configure DNS for {{ .Values.ingress.host }} to reach your ingress controller.
Then visit: {{ if .Values.ingress.tls.enabled }}https{{ else }}http{{ end }}://{{ .Values.ingress.host }}/
{{- else }}
Ingress is disabled. Route your configured HTTPS reverse proxy to the backend
and frontend Services. The AgentCore testing deployment uses the Caddy manifest
in deploy/agentcore-product/access.yaml.
{{- end }}

Inspect:
  kubectl -n {{ .Release.Namespace }} get pods,svc,ingress
  kubectl -n {{ .Release.Namespace }} logs deploy/{{ include "cubeplex.backend.fullname" . }}
